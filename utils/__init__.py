"""
Shared utility modules for the HR Attrition Prediction application.

Modules:
    database   — SQLite connection, schema, CRUD (Phase 2 — complete)
    train_model — Offline model training and .pkl export (Phase 3 — complete)
    preprocess — Feature encoding and scaling for inference (Phase 4 — complete)
    predictor  — Load model and return predictions (Phase 5 — complete)
    prediction_service — Predict + save to SQLite (Phase 7 — complete)
    history_dashboard — Format prediction history for UI (Phase 8 — complete)
"""
